/**
 * Keep the historic Pipeline route, but render the same authoritative
 * Postgres-backed acquisition console as /exec/marketing. The old page read
 * seeded clients and contradicted the live lead API.
 */
export { default } from "../marketing/page";
